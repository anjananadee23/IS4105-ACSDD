DATABASE_URL=./data/ceyloncart.db

# Demo-only defaults for the admin area (dummy login, not a real auth system).
# Set real values via a local .env file for anything beyond a local demo.
ADMIN_PASSWORD=admin123
ADMIN_SESSION_SECRET=dev-secret-change-me
